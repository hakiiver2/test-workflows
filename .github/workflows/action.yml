name: base

on:
  workflow_call:
    inputs:
      env:
        type: string
        required: true
      auto:
        type: string
        required: true
        default: 'false'
      pod:
        type: string
        required: false
        default: 'false'
env:
  TOKEN: ${{ secrets.TOKEN }}

jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: echo
        run: |
          echo "AUTO: ${{ inputs.auto }}"
          echo "POD: ${{ inputs.pod }}"
          echo "ENV: ${{ inputs.env }}"
          echo "TOKEN: ${TOKEN}"
          echo "VAL: ${{vars.VAL}}"
